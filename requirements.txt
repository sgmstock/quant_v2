# 量化交易系统 v2.0 依赖包
# Python 3.11+

# 核心数据处理与科学计算
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# 机器学习
scikit-learn>=1.3.0
xgboost>=1.7.0
lightgbm>=4.0.0
catboost>=1.2.0
tensorflow>=2.13.0  # 可选，深度学习
torch>=2.0.0  # 可选，深度学习

# 数据库交互
sqlalchemy>=2.0.0

# 数据源
akshare>=1.10.0
adata>=2.9.3  # 可选

# 回测框架
vectorbt>=0.25.0  # 可选
backtrader>=1.9.0  # 可选
zipline>=2.2.0  # 可选

# 可视化
streamlit>=1.28.0
plotly>=5.15.0
matplotlib>=3.7.0
seaborn>=0.12.0

# 技术分析
ta-lib>=0.4.25  # 可选，需要单独安装
pandas-ta>=0.3.14b0

# 配置管理
pyyaml>=6.0
python-dotenv>=1.0.0

# 日志
loguru>=0.7.0

# 日期时间处理
python-dateutil>=2.8.0

# 网络请求
requests>=2.31.0
aiohttp>=3.8.0  # 异步请求

# 数据处理
openpyxl>=3.1.0  # Excel文件处理
xlsxwriter>=3.1.0  # Excel写入

# 开发工具
pytest>=7.0.0
black>=23.0.0  # 代码格式化
flake8>=6.0.0  # 代码检查
mypy>=1.5.0  # 类型检查

# 性能优化
numba>=0.57.0  # JIT编译加速
cython>=0.29.0  # 可选，C扩展

# 时间序列分析
statsmodels>=0.14.0
arch>=6.0.0  # 金融时间序列分析

# 风险管理
pyfolio>=0.9.2  # 投资组合分析
empyrical>=0.5.5  # 金融指标计算

##  安装建议

### 基础安装（必需）
```bash
pip install pandas numpy scipy scikit-learn sqlalchemy akshare streamlit plotly pyyaml loguru python-dateutil requests
```

### 机器学习扩展
```bash
pip install xgboost lightgbm catboost
```

### 深度学习（可选）
```bash
pip install tensorflow torch
```

### 回测框架（可选）
```bash
pip install vectorbt backtrader
```

### 技术分析（可选）
```bash
pip install pandas-ta
# ta-lib 需要单独安装，参考官方文档
```

## 📝 更新说明

这个更新后的 `requirements.txt` 包含了：

1. **机器学习**：scikit-learn, xgboost, lightgbm, catboost
2. **深度学习**：tensorflow, torch（可选）
3. **回测框架**：vectorbt, backtrader, zipline
4. **技术分析**：pandas-ta, ta-lib
5. **可视化**：matplotlib, seaborn
6. **性能优化**：numba, cython
7. **风险管理**：pyfolio, empyrical

你希望我帮你更新 `quant_v2` 的 `requirements.txt` 文件吗？
